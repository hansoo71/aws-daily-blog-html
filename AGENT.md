# Agent Notes

- Keep `src/YYYYMMDD/` and `docs/YYYYMMDD/` synchronized.
- `docs/index.html` is the latest Pages entry and must rewrite image paths to `./YYYYMMDD/images/...`.
- Do not commit credentials or tokenized remotes.
- Do not upload generated HTML/images to OneDrive; OneDrive receives Markdown notes only.
