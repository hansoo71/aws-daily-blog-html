# Agent 운영 규칙

- HTML 표시 텍스트와 접근성 라벨은 한국어를 우선합니다.
- 원문 전체 추출본은 공개 저장소에 올리지 않습니다.
- 날짜별 `src/YYYYMMDD`와 `docs/YYYYMMDD`를 함께 유지합니다.
- 이미지 경로는 날짜별 페이지에서는 `./images/`, 최신 페이지에서는 `./YYYYMMDD/images/`를 사용합니다.
