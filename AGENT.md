# Agent Guide

- `src/YYYYMMDD/`를 원본으로 편집하고 `docs/YYYYMMDD/` 및 `docs/index.html`을 동기화한다.
- 원문 전체 복제본이나 비공개 자료는 public repo에 넣지 않는다.
- 이미지는 날짜별 `images/`에 보관하고 HTML 상대경로를 검증한다.
- 매 실행 후 로컬 HTML 구조 검증, git commit, GitHub Pages 검증을 수행한다.
