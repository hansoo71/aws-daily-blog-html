# 에이전트 편집 규칙

- 화면에 보이는 문구와 접근성 라벨은 한국어 존댓말·기업 블로그 톤을 유지합니다.
- 원문 링크는 보존하되 원문 전체 추출본은 공개 저장소에 올리지 않습니다.
- 날짜별 `src/`, `docs/`, 최신 `docs/index.html`을 함께 동기화합니다.
- 이미지 경로는 날짜별 문서에서는 `./images/`, 최신 문서에서는 `./YYYYMMDD/images/` 기준으로 검증합니다.
