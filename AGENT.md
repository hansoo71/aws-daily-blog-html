# AGENT

- `src/YYYYMMDD/`를 작업 원본으로, `docs/YYYYMMDD/`와 `docs/index.html`을 배포본으로 유지합니다.
- 표시 문구는 한국어 존댓말과 전문 블로그 톤을 우선합니다.
- 원문 URL, 제품명, 코드 식별자는 사실 검증을 위해 임의로 훼손하지 않습니다.
- 이미지 자산은 각 날짜의 `images/` 하위에 둡니다.
